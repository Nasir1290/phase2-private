import Subscription from '@/components/Subscription/Subscription';
import React from 'react';

const page = () => {
    return (
        <div>
            <Subscription/>
        </div>
    );
};

export default page;